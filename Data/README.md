# Data

```text
batches/       Raw annotation batch CSV files.
gold_data/     Gold labels, labeling comparison files, EDA outputs.
*.pdf, *.md    Labeling strategy/reference documents.
```

Notebook files live in `notebooks/`. Python workflows live in `scripts/`.
